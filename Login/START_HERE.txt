╔════════════════════════════════════════════════════════════════════════════╗
║                   🚀 LOGIN SYSTEM - PYTHON SOLUTION                         ║
║                     (Since Node.js won't install)                           ║
╚════════════════════════════════════════════════════════════════════════════╝

YOUR SITUATION:
===============
❌ Node.js installation not working
✅ Python is a better alternative for your setup
✅ Same login system, same database, just uses Python instead


IMMEDIATE ACTION - DO THIS NOW:
================================

1. INSTALL PYTHON
   ►Go: https://www.python.org/downloads/
   ►Download latest (blue button)
   ►Run installer
   ►CHECK "Add Python to PATH" ⭐ IMPORTANT!
   ►Click Install
   ►Restart computer


2. AFTER RESTART - OPEN COMMAND PROMPT
   ►Press: Windows Key + R
   ►Type: cmd
   ►Press: Enter


3. VERIFY PYTHON IS WORKING
   Type: python --version
   
   You should see: Python 3.x.x
   
   If error "not recognized", reinstall Python


4. NAVIGATE TO LOGIN FOLDER
   Copy Paste this:
   
   cd /d "d:\Storage Box\Computer Input\Visual Studio\Project\Shivani Anand\New folder\Login"
   
   Press Enter


5. INSTALL REQUIRED PACKAGES
   Copy Paste this:
   
   pip install flask flask-cors mysql-connector-python bcrypt
   
   Press Enter
   Wait 1-2 minutes for completion


6. CREATE MYSQL DATABASE
   ►Open MySQL Command Line or MySQL Workbench
   ►Copy/Paste this entire block:

   ═══════════════════════════════════════════════════════════
   CREATE DATABASE IF NOT EXISTS deepanshu;
   USE deepanshu;
   
   CREATE TABLE IF NOT EXISTS login (
       id INT AUTO_INCREMENT PRIMARY KEY,
       full_name VARCHAR(255) NOT NULL,
       email VARCHAR(255) NOT NULL UNIQUE,
       password VARCHAR(255) NOT NULL,
       created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
       updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
       INDEX idx_email (email)
   );
   ═══════════════════════════════════════════════════════════
   
   ►Press Enter
   ►Should see: Query OK


7. START THE SERVER
   In Command Prompt (in Login folder), type:
   
   python server_python.py
   
   Should show:
   ✓ Connected to MySQL Database 'deepanshu'
   Server running on: http://localhost:5000
   
   ⛔ LEAVE THIS WINDOW OPEN ⛔


8. TEST IN BROWSER
   ►Open web browser
   ►Go: http://localhost:5000/signup.html
   ►Fill form:
      Name: Test User
      Email: test@example.com
      Password: test123456
   ►Click: Sign Up
   
   ✓ Should say "registered successfully"


9. LOGIN TO TEST
   ►Go: http://localhost:5000/login.html
   ►Enter same email & password
   ►Click: Sign In
   
   ✓ Should show your profile!


🎉 SUCCESS! System is working!


IMPORTANT URLS:
===============
Sign Up: http://localhost:5000/signup.html
Login:   http://localhost:5000/login.html
Home:    http://localhost:5000/home.html


TO USE NEXT TIME:
=================
1. Open Command Prompt
2. Type: cd /d "d:\Storage Box\Computer Input\Visual Studio\Project\Shivani Anand\New folder\Login"
3. Type: python server_python.py
4. Go to: http://localhost:5000/signup.html


IF YOU GET STUCK:
==================

"Python not recognized"
→ Restart computer
→ Make sure you checked "Add Python to PATH" during install
→ Reinstall Python if needed

"pip install fails"
→ Close Command Prompt
→ Open new one
→ Try again

"Cannot connect to database"
→ Make sure MySQL is running
→ Make sure database 'deepanshu' was created
→ Check the SQL commands were pasted correctly

"Cannot GET /signup.html"
→ Make sure python server_python.py is running
→ Make sure using: http://localhost:5000 (NOT 3000)

"Registration fails after sign up"
→ Check server window for error messages
→ Make sure MySQL database exists
→ Try with a different email


FILES YOU NEED:
===============
✓ server_python.py    - The Python server (NEW!)
✓ login.html          - Login page (updated for port 5000)
✓ signup.html         - Sign up page (updated for port 5000)
✓ home.html           - Profile page (updated for port 5000)
✓ database.sql        - Database schema
✓ RUN_SERVER.bat      - Quick start script


SHORTCUTS:
===========
Double-click RUN_SERVER.bat to start everything after DB is created!


That's all! Your login system with MySQL database is ready to go! 🎉

Questions? Check PYTHON_SETUP_GUIDE.txt for more details.
